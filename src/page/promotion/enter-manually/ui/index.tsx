import { Flex } from '@/src/shared/ui/flex';

type Props = { promotionId: string };

export const PromotionEnterManuallyPage = ({ promotionId }: Props) => {
  return <Flex>Ввести данные вручную</Flex>;
};
