import { IWithdrawInfo } from '../model/withdraw-info.type';

import { Flex } from '@/src/shared/ui/primitives/flex';

type Props = {
  info: IWithdrawInfo;
};

export const WithdrawInfo = ({ info }: Props) => {
  return <Flex>withdraw</Flex>;
};
